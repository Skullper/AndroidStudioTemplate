package ${packageName}.fragment.presentation.presenter;

import ${packageName}.fragment.presentation.view.${viewName};

public class ${presenterName} extends BasePresenter<${viewName}> {

    public ${presenterName}(${viewName} view) {
	super(view);
    }

    @Override
    public void init(){
    }
}
